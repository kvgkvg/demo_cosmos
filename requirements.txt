# Python deps for infer_cosmos.py.
#
# Not installable with pip, but still required:
#   ffmpeg + ffprobe on PATH   frame sampling and chunk cutting
#
# build_demo.py needs nothing beyond the standard library.
# `python infer_cosmos.py --selfcheck` also runs with none of this installed.
#
# Verified by building a fresh conda env from this file alone and running the
# smoke test: Python 3.13, CUDA. Stock PyPI torch already carries sm_120, so
# RTX 50-series needs no --index-url.

torch>=2.4
torchvision>=0.19         # Qwen2VLImageProcessor imports it
torchcodec                # video decoding; transformers prefers it and the
                          # torchvision fallback is broken (no io.read_video)
transformers>=5.11        # Cosmos3OmniForConditionalGeneration
accelerate>=0.26          # device_map="auto"
pillow>=10                # Qwen2VLImageProcessor imports PIL
huggingface_hub>=0.34     # weight download, and the `hf` CLI
